apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-paperless
  namespace: mcp
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    geron.io/dns-scope: internal
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - secretName: mcp-paperless-vasary-tls
      hosts:
        - mcp-paperless.vasary.org
  rules:
    - host: mcp-paperless.vasary.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcp-paperless
                port:
                  name: http
