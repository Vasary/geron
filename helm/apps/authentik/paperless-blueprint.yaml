apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprint-paperless
  namespace: authentik
  labels:
    app.kubernetes.io/name: authentik
    app.kubernetes.io/component: blueprint
    blueprints.goauthentik.io/instantiate: "true"
data:
  paperless.yaml: |
    # yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
    version: 1
    metadata:
      name: vasary-paperless-proxy
      labels:
        blueprints.goauthentik.io/instantiate: "true"
    entries:
      - model: authentik_core.group
        id: paperless-users-group
        state: present
        identifiers:
          name: paperless-users
        attrs:
          name: paperless-users
      - model: authentik_providers_proxy.proxyprovider
        id: paperless-proxy-provider
        state: present
        identifiers:
          name: paperless
        attrs:
          name: paperless
          mode: forward_single
          grant_types:
            - authorization_code
          external_host: https://docs.vasary.org
          internal_host: http://paperless.paperless.svc.cluster.vasary.org
          internal_host_ssl_validation: true
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          cookie_domain: vasary.org
          redirect_uris:
            - matching_mode: strict
              url: https://docs.vasary.org/outpost.goauthentik.io/callback?X-authentik-auth-callback=true
              redirect_uri_type: authorization
            - matching_mode: strict
              url: https://docs.vasary.org?X-authentik-auth-callback=true
              redirect_uri_type: authorization
      - model: authentik_core.application
        id: paperless-application
        state: present
        identifiers:
          slug: paperless
        attrs:
          name: Paperless
          slug: paperless
          provider: !KeyOf paperless-proxy-provider
          policy_engine_mode: any
          meta_launch_url: https://docs.vasary.org/
          open_in_new_tab: true
      - model: authentik_policies.policybinding
        id: paperless-users-policy-binding
        state: present
        identifiers:
          target: !KeyOf paperless-application
          group: !KeyOf paperless-users-group
          order: 0
        attrs:
          target: !KeyOf paperless-application
          group: !KeyOf paperless-users-group
          order: 0
      - model: authentik_outposts.outpost
        id: authentik-embedded-outpost
        state: present
        identifiers:
          name: authentik Embedded Outpost
        attrs:
          providers:
            - !Find [authentik_providers_proxy.proxyprovider, [name, seasonvar]]
            - !KeyOf paperless-proxy-provider
