apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coins-inventory-redis-data-local
  namespace: coins-inventory
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
