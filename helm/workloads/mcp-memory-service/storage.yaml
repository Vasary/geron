apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mcp-memory-service-data
  namespace: mcp
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
