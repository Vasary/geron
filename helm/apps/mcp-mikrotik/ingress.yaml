apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-mikrotik
  namespace: mcp
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    geron.io/dns-scope: internal
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - secretName: mcp-mikrotik-vasary-tls
      hosts:
        - mcp-mikrotik.vasary.org
  rules:
    - host: mcp-mikrotik.vasary.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcp-mikrotik
                port:
                  name: http
