apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: seasonvar-postgres-local
  namespace: media
spec:
  instances: 1
  bootstrap:
    initdb:
      database: seasonvar
      owner: seasonvar
  storage:
    storageClass: local-path
    size: 1Gi
