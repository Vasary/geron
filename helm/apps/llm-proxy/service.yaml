apiVersion: v1
kind: Service
metadata:
  name: llm-proxy
  namespace: llm-proxy
spec:
  selector:
    app.kubernetes.io/name: llm-proxy
  ports:
    - name: http
      port: 80
      targetPort: http
