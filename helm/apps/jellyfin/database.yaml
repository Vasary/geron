apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: seasonvar-postgres
  namespace: media
spec:
  instances: 1
  bootstrap:
    initdb:
      database: seasonvar
      owner: seasonvar
  storage:
    storageClass: nfs
    size: 20Gi
