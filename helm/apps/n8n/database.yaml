apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: n8n-postgres
  namespace: n8n
spec:
  instances: 1
  bootstrap:
    initdb:
      database: n8n
      owner: n8n
  storage:
    storageClass: nfs
    size: 20Gi
