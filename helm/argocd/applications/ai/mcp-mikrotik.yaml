apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-mikrotik
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  project: ai-platform
  source:
    repoURL: https://github.com/Vasary/geron.git
    targetRevision: master
    path: helm/workloads/mcp-mikrotik
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
    syncOptions:
      - CreateNamespace=true
