apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcp-memory-service
  namespace: mcp
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    geron.io/dns-scope: internal
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - secretName: mcp-memory-service-vasary-tls
      hosts:
        - mcp-memory-service.vasary.org
  rules:
    - host: mcp-memory-service.vasary.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcp-memory-service
                port:
                  name: http
