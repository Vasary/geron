apiVersion: v1
kind: ConfigMap
metadata:
  name: authentik-blueprint-paperless
  namespace: authentik
  labels:
    app.kubernetes.io/name: authentik
    app.kubernetes.io/component: blueprint
    blueprints.goauthentik.io/instantiate: "true"
data:
  paperless.yaml: |
    # yaml-language-server: $schema=https://goauthentik.io/blueprints/schema.json
    version: 1
    metadata:
      name: vasary-paperless-oauth
      labels:
        blueprints.goauthentik.io/instantiate: "true"
    entries:
      - model: authentik_core.group
        id: paperless-users-group
        state: present
        identifiers:
          name: paperless-users
        attrs:
          name: paperless-users
      - model: authentik_providers_oauth2.oauth2provider
        id: paperless-oauth-provider
        state: present
        identifiers:
          name: paperless
        attrs:
          name: paperless
          client_type: confidential
          client_id: paperless
          client_secret: !Env [PAPERLESS_OAUTH_CLIENT_SECRET, ""]
          grant_types:
            - authorization_code
            - refresh_token
          authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-explicit-consent]]
          invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
          signing_key: !Find [authentik_crypto.certificatekeypair, [name, authentik Self-signed Certificate]]
          property_mappings:
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
            - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
          redirect_uris:
            - matching_mode: strict
              url: https://docs.vasary.org/accounts/oidc/authentik/login/callback/
          sub_mode: user_email
          issuer_mode: per_provider
      - model: authentik_core.application
        id: paperless-application
        state: present
        identifiers:
          slug: paperless
        attrs:
          name: Paperless
          slug: paperless
          provider: !KeyOf paperless-oauth-provider
          policy_engine_mode: any
          meta_launch_url: https://docs.vasary.org/
          open_in_new_tab: true
      - model: authentik_policies.policybinding
        id: paperless-users-policy-binding
        state: present
        identifiers:
          target: !KeyOf paperless-application
          group: !KeyOf paperless-users-group
          order: 0
        attrs:
          target: !KeyOf paperless-application
          group: !KeyOf paperless-users-group
          order: 0
