apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: paperless-postgres-local
  namespace: paperless
spec:
  instances: 1
  bootstrap:
    initdb:
      database: paperless
      owner: paperless
  storage:
    storageClass: local-path
    size: 10Gi
